keyWords = {
    "SELECT": "SELECT",
    "FROM": "FROM",
    "WHERE": "WHERE",
    "AND": "AND",
    "BETWEEN": "BETWEEN",
    "ORDER BY": "ORDER_BY",
    "ASC": "ASC",
    "LIMIT": "LIMIT",
}

ponctuation = {
    ".": "DOT",
    ",": "COMMA",
    "=": "IQUAL",
    ">": "MORE",
    "<": "LESS",
    "-": "DASH",
    "'": "APOSTROPHE",
}

wordSeparation = {
        " ": "WHITESPACE",
        "\n": "NEWLINE"
        }

number_re = r"[0-9]"

letter_re = r"[A-Za-z]"
