keyWords = {
        # "WORD": "TOKEN"
        "SELECT": "SELECT",
        "FROM": "FROM",
        "WHERE": "WHERE",
        "AND": "AND",
        "BETWEEN": "BETWEEN",
        "ORDER BY": "ORDER BY",
        "ASC": "ASC",
        "LIMIT": "LIMIT"
        }
